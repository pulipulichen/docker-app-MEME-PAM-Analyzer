version: "3"
services:
  app:
    build: .
    volumes:
      - "./app:/opt/app"
      - "./app/input:/opt/app/input"
    environment:
      #- TZ=${TZ}
      #- LANG=${LANG}
      - JUPYTER_TOKEN=password
    ports:
      - "20000:8888"